// Minimal stub for local EAS pre-flight check
// The actual native module is loaded by Expo's autolinking
const { withInfoPlist } = require('@expo/config-plugins');
module.exports = (config) => config;
